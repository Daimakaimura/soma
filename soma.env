# soma.env

# Agent backend: "opencode" | "claude" | "codex"
SOMA_AGENT=opencode

# Model for routine cycles (cheap, fast)
#   opencode: anthropic/claude-sonnet-4-20250514, google/gemini-2.5-pro,
#             ollama/qwen3-coder-30b (local!), openai/gpt-4o, etc.
#   claude:   sonnet, opus, haiku
#   codex:    (uses default)
SOMA_MODEL=anthropic/claude-sonnet-4-20250514

# Model for birth phase (thorough — worth spending more once)
SOMA_BIRTH_MODEL=anthropic/claude-opus-4-20250514

# Autonomy level: what the agent is allowed to do
#   observe    — read-only, report only (safest, good for first deployment)
#   suggest    — read-only commands + journal writes (recommended default)
#   act_safe   — low-risk corrective actions (restart services, clear temps)
#   act_full   — full authority (package updates, config changes, etc.)
SOMA_AUTONOMY=suggest

# How to reach the body:
#   "local"                   — commands run on this machine
#   "ssh://soma@1.2.3.4"     — commands run over SSH (prefer dedicated user over root)
SOMA_BODY=local

# Max prompt size in characters (adjust for model context window)
SOMA_MAX_PROMPT_CHARS=120000
